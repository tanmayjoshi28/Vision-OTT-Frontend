import { UrlTransformPipe } from './url-transform.pipe';

describe('UrlTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
